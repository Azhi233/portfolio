# Root environment variables
# Copy this file to `.env.local` or `.env` for local development.
# Vite client variables must start with `VITE_`.

# Frontend -> backend API proxy port
VITE_BACKEND_PORT=8787

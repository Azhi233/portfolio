# Root environment variables
# Copy this file to `.env.local` or `.env` for local development.
# Vite client variables must start with `VITE_`.

# API base URL used by the frontend request layer
# Leave this as `/api` for local development so Vite can proxy requests to the backend.
VITE_API_BASE_URL=/api

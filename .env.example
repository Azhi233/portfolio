# Root environment variables
# This project currently does not require root-level environment variables for local development.
# If you later add Vite client env values, they should start with VITE_.

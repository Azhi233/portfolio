services:
  minio:
    image: minio/minio:latest
    container_name: portfolio-minio
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: zhizhi
      MINIO_ROOT_PASSWORD: zhizhi233
    volumes:
      - minio_data:/data
    restart: unless-stopped

volumes:
  minio_data:
